<h1>Groups#index</h1>
<p>Find me in app/views/groups/index.html.erb</p>

<% @user_groups.each do |group| %>
	<%= link_to("#{group.name}", :controller => 'groups', :action => 'show', :id => group.id) %> <br />
<% end %>